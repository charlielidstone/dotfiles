---@type MappingsTable
local M = {}

M.general = {
  n = {
    [";"] = { ":", "enter command mode", opts = { nowait = true } },
  },
}

-- add ability to use caps lock instead of esc to exit insert mode:


-- more keybinds!

return M
